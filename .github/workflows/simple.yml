name: shell commands
on: [push]
jobs:
    run-shell-commands:
        runs-on: ubuntu-latest
        steps:
        - name: echo a string
          run: echo "Mohammed Salama"
        - name: multiline scripts
          run: |
            node -v
            npm -v

        
